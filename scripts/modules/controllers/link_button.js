var base = require('./base').default;
var button = require('./button').default;

export default createModuleDefinition([base, button], function($file_manager,$dialog, safeApply, pageEdit, pageEdit_layout) 
{
	"ngInject";
	
	
});
